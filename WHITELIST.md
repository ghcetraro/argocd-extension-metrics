# WHITELIST

- No commitear secretos reales en `manifests/` (usar `secret.example.yaml` como plantilla)
- No borrar `LICENSE`
- No modificar `extensions/**/yarn.lock` salvo actualización explícita de dependencias UI
- No forzar push a `main`/`master` sin pedido explícito del usuario
